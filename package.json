{
  "name": "bemm",
  "version": "0.5.0",
  "description": "Create BEM style classes",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "jest",
    "build": "rimraf dist && tsup --dts-resolve",
    "prepublishOnly": "npm run build && npm run test"
  },
  "exports": {
    ".": {
      "require": "./dist/index.js",
      "import": "./dist/index.mjs"
    }
  },
  "types": "./dist/index.d.ts",
  "keywords": [
    "Bem",
    "class",
    "js"
  ],
  "author": "Sil van Diepen",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^27.4.0",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.3",
    "typescript": "^4.5.5"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "tsup": "^6.3.0"
  }
}
